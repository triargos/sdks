{
  "name": "effect-procurat-v3-env",
  "version": "0.0.0",
  "private": true,
  "description": "Build and test environment for the generated Effect v3 SDK. Never published. Kept outside the pnpm workspace so its effect@3 tree cannot collide with the package's effect@4. Pinned to the floor of the published peer range, so CI fails on an API that only exists in a later v3 minor.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.generated.json",
    "build": "tsdown",
    "test": "vitest run"
  },
  "devDependencies": {
    "@effect/platform": "0.92.1",
    "@effect/vitest": "0.26.0",
    "@types/node": "^22.20.1",
    "effect": "3.18.5",
    "tsdown": "^0.22.14",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
